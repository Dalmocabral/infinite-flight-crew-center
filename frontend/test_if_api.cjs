const axios = require('axios'); const API_KEY = '36d1c8xdt1zvxn9cqqs9pxr7dty8rhm4'; const BASE_URL = 'https://api.infiniteflight.com/public/v2'; async function getFlights() { try { const userRes = await axios.post(BASE_URL + '/users?apikey=' + API_KEY, { discourseNames: ['Dalmo_Cabral'] }); const userId = userRes.data.result[0].userId; const flightRes = await axios.get(BASE_URL + '/users/' + userId + '/flights?apikey=' + API_KEY); console.log('Landing Stats:', flightRes.data.result.data[0].landingStats); } catch(e) { console.error('Error:', e.response ? e.response.data : e.message); } } getFlights();
